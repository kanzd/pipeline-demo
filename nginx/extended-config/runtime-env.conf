#  Provides the config.json config that is provided by TAP and generated by TAP Runtime Connector
location = /app-environment {
    try_files $uri $uri/ /config.json =404;
    add_header Cache-Control no-store;
}
